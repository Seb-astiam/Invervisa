import { Routes } from '@angular/router';
import { AddressesPageComponent } from './pages/addresses-page/addresses-page.component';

export const ADDRESSES_ROUTES: Routes = [
  { path: '', component: AddressesPageComponent },
];
